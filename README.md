# Color Palette Changer

This project aims to change the color palette of an input image using concepts from color theory, color clustering, and simple image processing.

The goal is to analyze the dominant colors in an image, reason about palette transformations, and apply those changes to produce a recolored output image.
